# vim: filetype=neomuttrc

color index white black
color index brightwhite black "~U"

# Define how the date itself should look (Year-Month-Day Hour:Minute)
set date_format = "%Y-%m-%d %H:%M"

# Apply it to your index view using %D for received date
set index_format = "%Z   %D   %-15.15F   %s"
